language: csharp
sudo: required

matrix:
  include:
    - name: Ubuntu
      dist: bionic
      addons:
        apt:
          packages:
            gettext
            libcurl4-openssl-dev
            libicu-dev
            libssl-dev
            libunwind8
            zlib1g
            clang
      mono: none
      dotnet: 2.1.508
    - name: macOS
      os: osx
      osx_image: xcode12
      mono: none
      dotnet: 2.1.508
script:
  dotnet restore